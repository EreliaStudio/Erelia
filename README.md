# Erelia V3

This repo root is now the live `V3` baseline.

Archived history:

- `archive/V1`: older archived implementation
- `archive/V2`: previous live implementation before the reset

Open this project with Unity `6000.3.1f1`.

Current V3 goal:

1. `MainMenu`
2. `Exploration`
3. `Battle`

Current V3 approach:

- rebuild the runtime loop cleanly
- keep UI simple
- use `AGENTS.md` and the wireframe PNGs as the source of truth
- treat `archive/V2` as reference only, not as the base to continue from
