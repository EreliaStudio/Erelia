# Exploration & World

See [global.md](global.md) for the epic summary.

| Priority | Pts | Ticket | Notes |
|----------|-----|--------|-------|
| 200 | 1 | **[EW-01] Subscribe `ModeManager` to `BattleStartRequested` event** | `ModeManager` must listen for `EventCenter.BattleStartRequested(EncounterData)` and call `SwitchMode(BattleMode)` passing the encounter data through. Currently the event is never consumed. |
| 200 | 2 | **[EW-02] Build board and enter `BattleMode` on `BattleStartRequested`** | Inside `ModeManager` (or `BattleMode.Enter`), call `BoardDataBuilder` with the player's current position and encounter data, then hand the result to `BattleOrchestrator`. Depends on BC-01 to BC-03. |
| 200 | 1 | **[EW-03] Return to `ExplorationMode` on `BattleEnded`** | `ModeManager` subscribes to `EventCenter.BattleEnded` and calls `SwitchMode(ExplorationMode)`. Restore the player's world position from `GameContext`. |
| 190 | 1 | **[EW-04] Subscribe `EncounterEmitter` to `EventCenter.EmitPlayerMoved`** | Single line: wire the existing `EncounterEmitter.OnPlayerMoved` handler to `EventCenter.PlayerMoved` in `ExplorationMode.Enter`. Unsubscribe in `Exit`. |
| 190 | 1 | **[EW-05] Query biome in `EncounterEmitter` for the encounter roll** | After the movement event triggers an encounter check, read the current chunk's `BiomeDefinition` from `WorldContext` and pass it to `EncounterResolver.Resolve(biome, tier)`. |
| 170 | 2 | **[EW-06] `TrainerActor` MonoBehaviour — sight range and encounter data** | Serialized fields: sight range (cells), facing direction, `EncounterData` reference, defeated flag. Placed in the world as a prefab. Registers itself with a scene-level trainer registry on `Awake`. |
| 170 | 2 | **[EW-07] Sight-line check in `TrainerActor.Tick`** | Each exploration tick, compute whether the player is within sight range and within the trainer's forward cone. Use a simple distance + dot-product check (no voxel raycasting needed for the prototype). |
| 170 | 1 | **[EW-08] Emit `BattleStartRequested` when player enters trainer sight** | If sight check passes and trainer is not defeated, call `EventCenter.EmitBattleStartRequested(encounterData)`. Mark trainer as "alerted" to prevent re-trigger mid-battle. |
| 145 | 2 | **[EW-09] Verify and complete `EncounterResolver` biome→table pipeline** | Trace `EncounterResolver.Resolve` end-to-end: biome → `BiomeEncounterRule` → weighted table → `EncounterTier` row → team composition roll. Fix any broken links. Add a test that a known biome returns a non-null encounter. |
| 145 | 1 | **[EW-10] Fallback to `defaultBiome` table in `EncounterResolver`** | If the current biome has no `BiomeEncounterRule` assigned, fall back to the world's `defaultBiome` encounter table rather than returning null or throwing. Log a warning. |
| 135 | 3 | **[EW-11] Perlin-noise biome assignment in `MetaWorldGenerator.GenerateChunkMeta`** | Replace `return defaultBiome` with a Perlin noise sample (using the world seed + chunk coordinates). Map noise ranges to `BiomeDefinition` assets via a configurable threshold list on `MetaWorldData`. |
| 135 | 1 | **[EW-12] Map noise thresholds to `BiomeDefinition` assets on `MetaWorldData`** | Add `List<BiomeNoiseThreshold>` (noise min, `BiomeDefinition`) to `MetaWorldData`. `GenerateChunkMeta` reads this list to select the biome for a given noise value. |
| 125 | 2 | **[EW-13] `HealPointEntity` MonoBehaviour — interactable, saves respawn position** | On player interaction, writes the current world position to `GameSaveData.LastHealPoint` and plays a confirmation effect. Presence gated on being in a town or designated safe zone. |
| 125 | 2 | **[EW-14] Wire defeat outcome to respawn at last heal point** | In `EndPhaseController`, when outcome is `Defeat`, emit `EventCenter.EmitPlayerDefeated`. `ExplorationMode` (or a `RespawnService`) listens and teleports the player to `GameSaveData.LastHealPoint`. |
| 115 | 2 | **[EW-15] Distance-based chunk unload in `WorldPresenter`** | After each player movement, mark chunks beyond an unload radius for destruction. Destroy their `GameObject` and remove from the active chunk registry. Use a separate, larger load radius to avoid thrashing. |
| 115 | 1 | **[EW-16] Hysteresis zone between load and unload radii** | Load radius = N chunks, unload radius = N + 2 chunks. Prevents a chunk from being loaded and immediately queued for unload when the player stands on the boundary. |
| 105 | 3 | **[EW-17] POI placement pass in `MetaWorldGenerator`** | Before chunk terrain generation, place towns and gyms at seeded positions across the world grid. Ensure minimum distance between POIs. Store POI positions in `MetaWorldData`. |
| 105 | 3 | **[EW-18] Voronoi-style road network connecting POIs** | Compute a minimum spanning tree (or Voronoi diagram) over POI positions. Mark road cells along each edge. Roads are stamped as a specific `VoxelDefinition` during chunk generation if the chunk overlaps a road edge. |
| 95 | 2 | **[EW-19] `InteriorTeleport` trigger volume MonoBehaviour** | Box collider trigger on a building entrance. On player enter, stores the exterior return position and loads the interior space (scene additive or dedicated interior voxel grid). |
| 95 | 2 | **[EW-20] Interior exit teleport back to exterior** | Interior scenes have a matching exit trigger that restores the exterior scene state and places the player at the saved return position from EW-19. |
| 75 | 2 | **[EW-21] Road voxel placement along generated paths** | During chunk mesh generation, replace terrain voxels with the road `VoxelDefinition` wherever the chunk overlaps a road edge from `MetaWorldData`. |
| 65 | 2 | **[EW-22] `AbilityGate` MonoBehaviour — checks required ability** | Serialized field: required ability name. On player interact, check if any team member has the ability unlocked. If yes, trigger the gate-open effect and mark as cleared in `GameSaveData`. |
| 45 | 2 | **[EW-23] Day/night cycle — sky color lerp** | Lerp the `RenderSettings.ambientLight` and skybox tint over a configurable cycle duration. Cosmetic only — no encounter variation. |
| 45 | 2 | **[EW-24] Weather particle system — rain / fog** | Simple particle system toggled by a `WeatherController` with configurable intensity. Cosmetic only. |
