using UnityEngine;

// This script file remains the MonoScript entry point used by prefabs.
// The compiled implementation lives in AttributesElementUI.cs so command-line builds
// keep working even when Unity has not refreshed the generated project files yet.
public sealed partial class ProgressBarElementUI : MonoBehaviour
{
}
