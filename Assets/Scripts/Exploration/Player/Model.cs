namespace Erelia.Exploration.Player
{
	/// <summary>
	/// Data model for the exploration player.
	/// Acts as a container for player exploration state (currently empty).
	/// </summary>
	/// <remarks>
	/// Currently empty but reserved for player state and configuration.
	/// </remarks>
	public sealed class Model
	{
	}
}
